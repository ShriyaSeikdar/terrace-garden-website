import 'dotenv/config'; import { PrismaClient } from './src/generated/prisma/client'; import { PrismaPg } from '@prisma/adapter-pg'; const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL as string }); const prisma = new PrismaClient({ adapter }); async function main() { const products = await prisma.product.findMany(); let count = 0; for (const product of products) { if (product.images && product.images.length > 0) { const firstImage = product.images[0]; const match = firstImage.match(/\/p(\d+)\.je?pg/i); if (match) { count++; console.log('Match:', match[1]); } else { console.log('No match for', firstImage); } } } console.log('Count:', count); } main().finally(() => prisma.$disconnect());

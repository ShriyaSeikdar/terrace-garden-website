import 'dotenv/config'; import { PrismaClient } from './src/generated/prisma/client'; import { PrismaPg } from '@prisma/adapter-pg'; const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL as string }); const prisma = new PrismaClient({ adapter }); prisma.product.findMany().then(p => console.log('Products:', p.length, p[0]?.images)).finally(() => prisma.$disconnect());
